query GetPosts($post_id: String!) {
  getPost(post_id: $post_id) {
    post_id
    title
    description
    picture
    created_at
    creator_id
    creator {
      user_id
      username
      picture
    }
  }
}
