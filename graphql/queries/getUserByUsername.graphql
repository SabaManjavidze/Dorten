query GetUserByUsername($username: String!) {
  getUserByUsername(username: $username) {
    user_id
    age
    picture
    gender
    posts {
      post_id
      title
      description
      picture
      points
      created_at
    }
  }
}
