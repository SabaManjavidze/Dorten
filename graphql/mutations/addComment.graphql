mutation addComment($postId: String!, $text: String!) {
  addComment(postId: $postId, text: $text) {
    post_id
    comment_id
    creator_id
    creator {
      ...UserFragment
    }
    created_at
  }
}
