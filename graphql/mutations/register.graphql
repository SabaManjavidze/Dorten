mutation Register($options: UserCreateInput!) {
  register(options: $options) {
    user {
      username
      email
      picture
      gender
      age
    }
    errors {
      field
      message
    }
  }
}
